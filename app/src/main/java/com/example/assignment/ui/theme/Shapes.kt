package com.example.assignment.ui.theme

import androidx.compose.material.Shapes
import androidx.compose.foundation.shape.RoundedCornerShape
import androidx.compose.ui.unit.dp

// Define your shapes here
val Shapes = Shapes(
    small = RoundedCornerShape(4.dp),   // Small shapes (e.g., buttons)
    medium = RoundedCornerShape(8.dp),  // Medium shapes (e.g., cards)
    large = RoundedCornerShape(16.dp)    // Large shapes (e.g., dialogs)
)
